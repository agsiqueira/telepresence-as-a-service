"use client";
import { useEffect,useState } from "react";
type Proposal={id:string;journeyRequestId:string;version:number;status:string;currency:string;proposedPriceMinor:number};
export default function ProposalAdminList(){const[proposals,setProposals]=useState<Proposal[]>([]),[message,setMessage]=useState("");useEffect(()=>{void fetch("/api/admin/proposals",{cache:"no-store"}).then(async response=>{const data=await response.json();if(!response.ok)throw new Error(data.error??"Unable to load Proposals");setProposals(data.proposals??[])}).catch(error=>setMessage(error.message))},[]);return <main className="mx-auto max-w-5xl px-4 py-10"><h1 className="text-3xl font-bold">Proposal activity</h1><p className="mt-2 text-gray-600">Safe operational visibility without private location or profile information.</p>{message&&<p role="alert">{message}</p>}<ul className="mt-6 divide-y rounded-xl border bg-white">{proposals.map(proposal=><li key={proposal.id} className="p-4"><p className="font-semibold">Request {proposal.journeyRequestId} · Version {proposal.version}</p><p className="text-sm">{proposal.status} · {proposal.proposedPriceMinor} {proposal.currency}</p></li>)}</ul></main>}
