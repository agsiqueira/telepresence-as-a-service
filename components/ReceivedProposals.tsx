"use client";
import { useCallback,useEffect,useState } from "react";
type Proposal={id:string;teleporterName:string;version:number;status:string;earliestStart:string;durationMinutes:number;proposedPriceMinor:number;currency:string};
export default function ReceivedProposals({requestId}:{requestId:string}){const[proposals,setProposals]=useState<Proposal[]>([]),[message,setMessage]=useState("");const load=useCallback(async()=>{const response=await fetch(`/api/journey-requests/${requestId}/proposals`,{cache:"no-store"});const data=await response.json();if(!response.ok)throw new Error(data.error??"Unable to load Proposals");setProposals(data.proposals??[])},[requestId]);useEffect(()=>{void load().catch(error=>setMessage(error.message))},[load]);async function decline(id:string){const response=await fetch(`/api/journey-requests/${requestId}/proposals/${id}/decline`,{method:"POST"});const data=await response.json();if(!response.ok)return setMessage(data.error??"Unable to decline Proposal");setMessage("Proposal declined and preserved in history.");await load()}return <section className="mx-auto mt-8 max-w-2xl px-4 pb-10"><h2 className="text-xl font-semibold">Received Proposals</h2><p className="mt-1 text-sm text-gray-600">Confirmation is not available until the Agreement phase.</p>{message&&<p role="status" className="mt-3">{message}</p>}{proposals.length===0?<p className="mt-3 text-gray-500">No Proposals received.</p>:<ul className="mt-3 divide-y rounded-xl border bg-white">{proposals.map(proposal=><li key={proposal.id} className="p-4"><p className="font-semibold">{proposal.teleporterName} · Version {proposal.version}</p><p className="text-sm">{proposal.status} · {proposal.durationMinutes} min · {proposal.proposedPriceMinor} {proposal.currency}</p><p className="text-sm">Starts {new Date(proposal.earliestStart).toLocaleString()}</p>{proposal.status==="ACTIVE"&&<button onClick={()=>void decline(proposal.id)} className="mt-3 min-h-11 rounded-lg border border-red-700 px-4 text-red-800">Decline Proposal</button>}</li>)}</ul>}</section>}
