import assert from "node:assert/strict";import{readFileSync}from"node:fs";const read=p=>readFileSync(new URL(`../${p}`,import.meta.url),"utf8");const schema=read("prisma/schema.prisma"),migration=read("prisma/migrations/20260801060000_phase5d1_bilateral_journey_reviews/migration.sql"),service=read("lib/journey-reviews.ts"),lifecycle=read("lib/trip-lifecycle.ts"),route=read("app/api/trips/[id]/review/route.ts"),feedback=read("app/api/feedback/route.ts");
assert.match(schema,/enum JourneyReviewRole[\s\S]*EXPLORER[\s\S]*TELEPORTER/);assert.match(schema,/model JourneyReview/);assert.match(schema,/reviewDeadlineAt\s+DateTime\?/);assert.match(schema,/@@unique\(\[tripId, reviewerId\]\)/);assert.match(schema,/@@unique\(\[tripId, reviewerRole\]\)/);for(const token of ["JourneyReview_rating_check","JourneyReview_distinct_participants_check","JourneyReview_opposite_roles_check","JourneyReview_comment_length_check","JourneyReview_prevent_update","JourneyReview_prevent_delete","Trip_reviewDeadline_immutable"])assert.match(migration,new RegExp(token));assert.doesNotMatch(migration,/UPDATE "Trip"|UPDATE "Feedback"|ALTER TABLE "Feedback"/);assert.match(lifecycle,/REVIEW_WINDOW_MS = 14 \* 24/);assert.match(lifecycle,/reviewDeadlineAt: reviewDeadline\(now\)/);assert.match(service,/FOR UPDATE/);assert.match(service,/CURRENT_TIMESTAMP/);assert.match(service,/both\|\|now>=trip\.reviewDeadlineAt/);assert.match(service,/getRoleReputation/);assert.match(service,/revieweeRole:performedRole/);assert.doesNotMatch(service,/feedback|SafetyReport|\bTip\b/);assert.match(route,/authorizeApiUser/);assert.doesNotMatch(route,/reviewerId|revieweeId|reviewerRole|revieweeRole/);assert.match(feedback,/completeViewerFeedback/);console.log("Phase 5D.1 bilateral-review structural validation passed: 25/25");
