import assert from "node:assert/strict";
import { readFileSync } from "node:fs";
const read = path => readFileSync(path, "utf8"); let passed = 0; const check = (value, label) => { assert.ok(value, label); passed += 1; };
const page = read("app/operator/account/page.tsx"), profile = read("components/ProfileSettings.tsx"), overview = read("components/operator/TeleporterAccountOverview.tsx"), operatorApi = read("app/api/operator/profile/route.ts"), viewerApi = read("app/api/viewer/profile/route.ts"), settingsApi = read("app/api/operator/settings/route.ts"), root = read("app/layout.tsx"), layout = read("app/operator/layout.tsx"), home = read("app/operator/page.tsx"), requests = read("components/JourneyRequestDiscovery.tsx") + read("components/ProposalManager.tsx"), journeys = read("components/TeleporterAgreements.tsx"), offerings = read("components/LiveMomentManager.tsx") + read("components/GuidedExperienceManager.tsx"), pkg = read("package.json"), presentation = page + profile + overview;
check(page.includes('title="Account"') && page.includes('eyebrow="Teleporter"'), "canonical Account header retained"); check(page.includes("AccountSafetyRestrictionNotice"), "Safety restriction notice retained"); check(profile.includes("`/api/${role}/profile`") && operatorApi.includes("displayName"), "Operator profile endpoint retained"); check(profile.includes('role === "viewer" ? { displayName, preferredLanguage, accessibilityPreferences } : { displayName }'), "Operator PUT remains display-name-only"); check(profile.includes('new Event("operator-profile-updated")'), "Operator profile update event retained"); check(profile.includes('role === "viewer"') && profile.includes("preferredLanguage") && profile.includes("accessibilityPreferences"), "Explorer preferences retained"); check(!operatorApi.includes("preferredLanguage") && !operatorApi.includes("accessibilityPreferences"), "Operator endpoint remains narrow");
for (const state of ['"loading"', '"ready"', '"unauthorized"', '"failed"', "Retry profile", "Saving…", 'kind: "success"', 'kind: "error"']) check(profile.includes(state), `profile state represented: ${state}`); check(profile.includes("saving.current") && profile.includes("if (saving.current) return") && profile.includes("disabled={isSaving}"), "duplicate profile save protection retained");
check(overview.includes('fetch("/api/access-state"') && overview.includes('fetch("/api/operator/settings"'), "authoritative Account data used"); for (const token of ["Account status", "Pilot status", "Operational readiness", "settings.readiness.eligible", "settings.readiness.message", "settings.complete"]) check(overview.includes(token), `distinct status/readiness value rendered: ${token}`); check(settingsApi.includes("evaluateOperatorReadiness") && !overview.includes("evaluateOperatorReadiness"), "readiness remains server-computed"); check(overview.includes("Read-only summary") && !overview.includes('method: "PUT"'), "capabilities remain read-only"); check(overview.includes('href="/operator"') && overview.includes("Edit service setup on Home"), "Home remains service setup editor"); for (const field of ["operatingArea", "serviceRadiusKm", "destinationIds", "languages", "accessibilityCapabilities", "durationOptions", "supportsCustom"]) check(overview.includes(field), `capability summary uses existing field: ${field}`);
check(overview.includes('href="/viewer/operator-application"'), "existing application route retained"); check(!overview.includes("OperatorApplicationViewer") && !overview.includes("qualifications"), "full application form not duplicated"); check(page.includes('href="/safety-support"') && page.includes("Open Safety support"), "Safety support retained"); check(root.includes("<UserButton />") && page.includes("global header"), "Clerk account control remains global"); check(!/SignOutButton|signOut\(|useClerk|clerkId|session token/i.test(presentation), "custom authentication absent");
for (const source of [profile, overview]) { check(source.includes('role="status"') && source.includes('role="alert"'), "loading/success and failure semantics present"); check(source.includes("break-words") || source.includes("w-full"), "responsive wrapping marker present"); } check(presentation.includes("sm:grid-cols-3") && presentation.includes("flex-col") && presentation.includes("sm:flex-row"), "Account responsive hierarchy present");
check(layout.includes('{ label: "Home"') && layout.includes('{ label: "Requests"') && layout.includes('{ label: "Journeys"') && layout.includes('{ label: "Offerings"') && layout.includes('{ label: "Account"'), "Phase 7C.1 navigation unchanged"); check(home.includes("AvailabilityCard"), "Phase 7C.2 Home behavior retained"); check(requests.includes("Proposal history"), "Phase 7C.3 Requests retained"); check(journeys.includes("Confirmed Journeys"), "Phase 7C.4 Journeys retained"); check(offerings.includes("Live Moments") && offerings.includes("Guided Experiences"), "Phase 7C.5 Offerings retained"); check(viewerApi.includes("preferredLanguage") && viewerApi.includes("accessibilityPreferences"), "Explorer profile API unchanged"); check(pkg.includes('"test:phase7c6": "node scripts/validate-phase7c6-teleporter-account.mjs"'), "package script registered");
for (const prohibited of ["Go online", "Immediate Journey offer", "JourneyRequestDiscovery", "ProposalManager", "TeleporterAgreements", "JourneyReschedulingPanel", "VideoRoom", "LiveMomentManager", "GuidedExperienceManager", "payment", "payout", "Tip status", "Teleporter rating", "JourneyReviewPanel", "earnings", "notification preference", "Delete account", "Deactivate account", "role switch", "localStorage"]) check(!presentation.toLowerCase().includes(prohibited.toLowerCase()), `unsupported Account behavior absent: ${prohibited}`);
console.log(`PASS Phase 7C.6 Teleporter Account hierarchy, profile ownership, readiness, capabilities, support, accessibility, and scope validation: ${passed}/${passed}`);
