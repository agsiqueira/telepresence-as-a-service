import {spawnSync} from "node:child_process";import{cpSync,mkdirSync,readFileSync,rmSync,writeFileSync}from"node:fs";
const p3=process.env.PHASE3_TEST_DATABASE_URL,p4=process.env.PHASE4_TEST_DATABASE_URL;if(!p3||!p4)throw new Error("Both test database URLs are required");if(p3!==p4)throw new Error("Test database URLs differ");const configured=(readFileSync(".env","utf8").split(/\r?\n/).find(value=>/^DATABASE_URL\s*=/.test(value))||"").replace(/^DATABASE_URL\s*=\s*/,"").trim().replace(/^['\"]|['\"]$/g,"");if(!configured||p3===configured)throw new Error("Disposable test database must differ from DATABASE_URL");const env={...process.env,DATABASE_URL:p3};
const compile=spawnSync(process.execPath,["node_modules/typescript/bin/tsc","-p","scripts/tsconfig.phase3-db.json"],{stdio:"inherit",env});if(compile.status!==0)process.exit(compile.status??1);
for(const module of["agreements","journey-requests","marketplace","marketplace-vocabulary","profiles","safety-restriction-lock","supply-foundation"]){const path=`.phase3-test-build/lib/${module}.js`;writeFileSync(path,readFileSync(path,"utf8").replace('require("server-only");',""))}const aliasRoot=".phase3-test-build/node_modules/@/lib";mkdirSync(aliasRoot,{recursive:true});for(const module of["agreements","journey-requests","marketplace","marketplace-vocabulary","profiles","safety-restriction-lock","supply-foundation"])cpSync(`.phase3-test-build/lib/${module}.js`,`${aliasRoot}/${module}.js`);
const test=spawnSync(process.execPath,[".phase3-test-build/scripts/phase5a1-db-integration.js"],{stdio:"inherit",env});rmSync(".phase3-test-build",{recursive:true,force:true});process.exit(test.status??1);
