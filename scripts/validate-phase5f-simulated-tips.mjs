import assert from "node:assert/strict";import{readFileSync}from"node:fs";
const read=p=>readFileSync(p,"utf8"),schema=read("prisma/schema.prisma"),migration=read("prisma/migrations/20260802020000_phase5f_simulated_tips/migration.sql"),service=read("lib/simulated-tips.ts"),route=read("app/api/trips/[id]/simulated-tip/route.ts"),ui=read("components/SimulatedTipPanel.tsx"),review=read("components/JourneyReviewPanel.tsx");
for(const token of["model SimulatedTip","tripId       String   @unique","@@unique([tripId, explorerId, teleporterId])","@db.Char(3)","@db.Timestamptz(3)","SimulatedTipsGiven","SimulatedTipsReceived"])assert.match(schema,new RegExp(token.replace(/[.*+?^${}()|[\]\\]/g,"\\$&")));
for(const token of["SimulatedTip_amount_check","SimulatedTip_currency_check","SimulatedTip_validate_journey","SimulatedTip_prevent_update","SimulatedTip_prevent_delete","SimulatedTip_trip_participants_fkey","FOR KEY SHARE"])assert.match(migration,new RegExp(token));
assert.doesNotMatch(migration,/\b(?:UPDATE|DELETE FROM|INSERT INTO)\s+"(?:Trip|Agreement|JourneyReview|Feedback|SafetyReport|User)"/i);assert.doesNotMatch(schema+migration,/Stripe|processor|payout|settlement|chargeback|paymentMethod/i);
for(const token of["SIMULATED_TIP_AMOUNTS","500,1000,1500,2000","currency:\"USD\"","acquireSafetyRestrictionParticipantLocks","hasEffectiveSafetyRestrictionInTransaction","AccountStatus.ACTIVE","FOR UPDATE","SIMULATED_TIP_ALREADY_SUBMITTED","SIMULATED_TIP_CHANGED_CONCURRENTLY","publicDisplayName"])assert.match(service,new RegExp(token.replace(/[.*+?^${}()|[\]\\]/g,"\\$&")));
assert.match(route,/Object\.keys\(input\)\.length!==1/);assert.match(route,/getCurrentPersistedUser/);assert.doesNotMatch(route,/authorizeApiUser/);
for(const token of["500,1000,1500,2000","This is a simulation. No money will be charged or transferred.","type=\"radio\"","aria-live","required","No simulated Tip has been recorded","cannot be edited, replaced, withdrawn, or deleted"])assert.match(ui,new RegExp(token.replace(/[.*+?^${}()|[\]\\]/g,"\\$&")));
assert.match(review,/SimulatedTipPanel/);assert.doesNotMatch(ui,/Viewer|Operator|JourneyReview|reputation|Feedback|SafetyReport|Agreement|Stripe/i);console.log("PASS Phase 5F structural, service/API/privacy, UI and accessibility validation");
