import{NextRequest,NextResponse}from"next/server";import{db}from"@/lib/db";import{authorizeExplorerApi,enforceAccountSafetyForActivity}from"@/lib/current-user";import{transitionGuidedExperience}from"@/lib/guided-experiences";import{guidedExperienceError}from"@/lib/guided-experience-api";
export async function POST(request:NextRequest,{params}:{params:{id:string;action:string}}){const actor=await authorizeExplorerApi();if(!actor.ok)return actor.response;const restricted=await enforceAccountSafetyForActivity(actor.user.id);if(restricted)return restricted;if(!["publish","pause","resume","archive"].includes(params.action))return NextResponse.json({error:"NOT_FOUND"},{status:404});try{return NextResponse.json({guidedExperience:await transitionGuidedExperience(db,actor.user.id,params.id,params.action as "publish"|"pause"|"resume"|"archive",await request.json())},{headers:{"Cache-Control":"no-store"}})}catch(error){return guidedExperienceError(error)}}
