import{NextResponse}from"next/server";import{getCurrentPersistedUser}from"@/lib/current-user";import{db}from"@/lib/db";import{getSafetyConversationForParticipant,SafetyConversationError}from"@/lib/safety-conversations";export async function GET(_:Request,{params}:{params:{conversationId:string}}){const user=await getCurrentPersistedUser();if(!user)return NextResponse.json({error:"Unauthenticated"},{status:401});try{return NextResponse.json({conversation:await getSafetyConversationForParticipant(db,user.id,params.conversationId)})}catch(e){return NextResponse.json({error:"Safety conversation not found"},{status:e instanceof SafetyConversationError?e.status:500})}}
